# Deletion Service

Handles all data deletion operations including localStorage, sessionStorage, and IndexedDB cleanup.

## API

`DeletionService.deleteAllData()` - Clears all application data and reloads the page
`DeletionService.deleteAllModels()` - Removes cached model data from localStorage
